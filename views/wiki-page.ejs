<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %> - CodeWiki</title>
  <link rel="stylesheet" href="/public/style.css">
</head>
<body>
  <div class="container wiki-page">
    <header>
      <h1><%= title %></h1>
      <nav class="breadcrumb">
        <a href="/">Dashboard</a> /
        <a href="/wiki/<%= project %>/index">Wiki</a> /
        <span><%= title %></span>
      </nav>
    </header>

    <main>
      <article class="wiki-content">
        <%- page.content %>
      </article>

      <% if (page.metadata && page.metadata.related && page.metadata.related.length > 0) { %>
      <aside class="related-pages">
        <h3>Related Pages</h3>
        <ul>
          <% page.metadata.related.forEach(relatedPath => { %>
            <li><a href="/wiki/<%= project %>/<%= relatedPath.replace('.md', '') %>"><%= relatedPath %></a></li>
          <% }); %>
        </ul>
      </aside>
      <% } %>
    </main>

    <footer>
      <a href="/">← Back to Dashboard</a>
    </footer>
  </div>
</body>
</html>
